function Game() {
  var self = this;

  self.platforms = [];
  self.players = [];
  self.camera = null;

  self.init = function() {
  };

  self.tick = function(dt) {
  };

  self.render = function (dt) {
  };
}
