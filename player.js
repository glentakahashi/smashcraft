function Player() {
  var self = this;
  var model = null;

  // Game variables
  self.loc = mat4(0.0, 0.0, 0.0, 1.0);
  self.health = 100;
  self.animation = null;
  
  self.init = function() {
  };

  self.tick = function(dt) {
  };

  self.render = function (dt) {
  };
}
